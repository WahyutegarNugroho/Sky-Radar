import { describe, it, expect, vi, afterEach } from 'vitest';
import { renderHook, waitFor } from '@testing-library/react';
import { useWeather } from '../../hooks/useWeather'; const mockGetClientCache = vi.fn(() =>null);
const mockSetClientCache = vi.fn(); vi.mock('../../lib/client-cache', () =>({ getClientCache: (...args) =>mockGetClientCache(...args), setClientCache: (...args) =>mockSetClientCache(...args),
})); const mockWeatherData = { temperature_2m: 25, weather_code: 0 }; describe('useWeather', () =>{ afterEach(() =>{ vi.restoreAllMocks(); global.fetch?.mockRestore?.(); }); it('returns null states when coords are null', () =>{ const { result } = renderHook(() =>useWeather(null, null)); expect(result.current.loading).toBe(false); expect(result.current.weather).toBeNull(); expect(result.current.error).toBeNull(); }); it('fetches weather data and sets state', async () =>{ global.fetch = vi.fn().mockResolvedValue({ ok: true, json: () =>Promise.resolve(mockWeatherData) }); const { result } = renderHook(() =>useWeather(-6.2, 106.8)); await waitFor(() =>expect(result.current.loading).toBe(false), { timeout: 3000 }); expect(result.current.weather).toEqual(mockWeatherData); }); it('sets error on fetch failure', async () =>{ global.fetch = vi.fn().mockResolvedValue({ ok: false, status: 500 }); const { result } = renderHook(() =>useWeather(-6.2, 106.8)); await waitFor(() =>expect(result.current.loading).toBe(false), { timeout: 3000 }); expect(result.current.error).toContain('500'); }); it('uses cached data when available', () =>{ global.fetch = vi.fn(); mockGetClientCache.mockReturnValue(mockWeatherData); const { result } = renderHook(() =>useWeather(-6.2, 106.8)); expect(result.current.weather).toEqual(mockWeatherData); expect(result.current.loading).toBe(false); expect(global.fetch).not.toHaveBeenCalled(); });
});
